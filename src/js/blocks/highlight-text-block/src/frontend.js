/**
 * src/js/blocks/highlight-text-block/src/frontend.js
 * (блок статический, JS здесь не требуется)
 */
export default function initHighlightText() {
  // ничего
}

window.addEventListener('DOMContentLoaded', initHighlightText);
