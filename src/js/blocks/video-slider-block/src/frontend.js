/**
 * Placeholder for slider initialization (e.g. Swiper, Slick)
 */
export default function initVideoSlider() {
  console.log('BIOTROPIKA Video Slider initialized');
}

window.addEventListener('DOMContentLoaded', initVideoSlider);
