/**
 * Партнеры — без JS‑поведения сейчас
 */
export default function initPartnersGrid() {
  // если понадобится интерактивность — тут оживим
}

window.addEventListener('DOMContentLoaded', initPartnersGrid);
